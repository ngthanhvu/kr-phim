# name: Deploy Production

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: [self-hosted, linux]

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Stop old containers
#         run: |
#           cd /root/www/kr-phim
#           docker stop cinek 2>/dev/null || true
#           docker rm cinek 2>/dev/null || true

#       - name: Build production image
#         run: |
#           cd /root/www/kr-phim
#           docker build -t cinek:latest -f docker/Dockerfile.prod ..

#       - name: Start MySQL & Redis
#         run: |
#           cd /root/www/kr-phim
#           docker compose -f docker/docker-compose.prod.yml up -d mysql redis

#       - name: Wait for database
#         run: sleep 5

#       - name: Start application container
#         run: |
#           cd /root/www/kr-phim
#           docker compose -f docker/docker-compose.prod.yml up -d cinek

#       - name: Verify deployment
#         run: |
#           sleep 10
#           cd /root/www/kr-phim
#           docker compose -f docker/docker-compose.prod.yml ps

#       - name: Cleanup unused images
#         run: docker system prune -f --volumes || true
